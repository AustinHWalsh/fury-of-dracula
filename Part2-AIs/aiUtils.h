//declare functions here

// get a random number in the range of an array length
// uses a function to determine the randomness
int randomRange(int arrayLen);

// convert an abbreviation from const char* to 
// a string (char *)
char *convertAbbrev(const char *abbrev);